import TransferModal from "./TransferModal";

const TransferRegistryModal = (props) => {
  return (
    <TransferModal
      {...props}
      title="dashboard.registryOverview.fields.transferTitle"
    />
  );
};

export default TransferRegistryModal;
