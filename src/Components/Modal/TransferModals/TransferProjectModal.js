import TransferModal from "./TransferModal";

const TransferProjectModal = (props) => {
  return (
    <TransferModal
      {...props}
      title="dashboard.projectOverview.fields.transferTitle"
    />
  );
};

export default TransferProjectModal;
