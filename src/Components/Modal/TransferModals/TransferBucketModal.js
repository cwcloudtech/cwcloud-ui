import TransferModal from "./TransferModal";

const TransferBucketModal = (props) => {
  return (
    <TransferModal
      {...props}
      title="dashboard.bucketOverview.fields.transferTitle"
    />
  );
};

export default TransferBucketModal;
